//CONDICIONAIS:
//IF, ELSE, IF ELSE
//SWITCH CASE

let par = 8

if (par>=10 ){
    console.log("O número é maior que 10!");
} else {
    console.log("O número é menor do que 10!");
};


switch(variavel) {
    case 1: 
    blocodecondicao;
    break; 
    default:
        // trato a exceção (defalt)
}